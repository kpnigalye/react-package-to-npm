# Setup to publish a React package to npm

This setup includes the following two projects.

- `react-counter` react project to publish as an npm package
- `test-react-counter` react project to test this npm package